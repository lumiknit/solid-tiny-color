import { Component } from "solid-js";

type Props = {
	min: number;
	max: number;

	value: number;
	onValueChange: (value: number) => void;
};

const Slider: Component<Props> = (props) => {
	return (
		<div>

		</div>
	);
};
